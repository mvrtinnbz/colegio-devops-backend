spring:
  cloud:
    config:
      enabled: false
      discovery:
        enabled: false
  config:
    import: "" 
eureka:
  client:
    enabled: false
    register-with-eureka: false
    fetch-registry: false